version: '3.8'
services:
  frontend:
    build: .
    ports:
      - '3000:3000'
    environment:
      # Next.js /api/contact should be handled by the Next.js app itself (deployed on Vercel).
      # When running via Docker locally, you can override if needed.
      NEXT_PUBLIC_API_BASE: ""
